background:
-  we are developing the bcsv library. The Binary-CSV (BCSV) library that tries to combine the flexibility of CSV-files with the speed and size efficency of a binary format. See README.md and ARCHITECTURE.md ffor background.
- bcsv sources are located in ./include/bcsv
- examples demonstrating usage of the BCSV, also used for debugging and quality assurance are located in ./examples
- test are located in ./tests
- ./tmp provides space for temporary experiments and runs and is not version controlled (ignored) by git
- we are using cmake & ninja for builds
- we are using google test for tests ./tests/bcsv_gtest
- benchmark suite is located under: ./benchmark (README.md has full docs)
- documentation (also on the API) is provided here: ./docs

[x] 0. Test and Stabilize direct access feature
[x] 1. Test and Stabilize CLI tools:
[x] 4. Refactor VLE (Variabel Length Encoding):
[x] 5. Consider a robust handling of ColumnNames. 
[x] 6. Modify Row and Layout to not use std::variants, consider alignment
[x] 7. Refactor bitset_dynamic to use full register width instead of narrow bytes. Thus we want to use 64bit types on 64bit platform. Consider alignment.
[x] 8. Add visit() function to Row, RowStatic, RowView, RowViewStatic enable fast operations on the data 
[x] 8.b: Exploit visit function to simplify code [EVALUATED - NO ACTION NEEDED]
[x] 8.c: Refeactoring Row, Layout relation ship. 
[x] 8.d: Finalize Row::onAddColumn, Row::onChangeColumnType, Row::onRemoveColumn
[x] 8.e: debug large column count bools
[x] 8.f: Add test for bitset insert
[x] 8.g: Improve bcsv::bitset:
[x] 8.h Make change tracking for Row and RowStatic a compile time decision.
[x] 8.h: Restructure Row object to not use ptr_. 
[x] 9. Improve benchmark suite
[x] 9.b Do improve efficency on AI Co-development create persitent information about the project, supports AI-agents to quickly onboard and understand the goals, codes and infrastrutcure.
[x] 10. Change "flat" (none-ZoH) wire format to optimize compression and read & write performance
[x] 10 (Fix). Harden Item 10 implementation (flat wire format)
[x] 11. Restructure Interaction between Row(classes) and Reader/Writer. Proposal: Introduce a Serializer / Deserializier layer within the API that is going to allow us to support different Encoding schemas, limiting the impact on Row, Reader and Writer classes.
[x] 11.B Clean-up, performance, hardening
[x] 11.C Benchmark — Reduce Complexity
[x] 11.D Benchmarks:
[x] 11.E Rework/optimize ZoH Codec, serialize and deserialize
[x] 11.F Add File Codec and Backward Compatibility
[x] 11.G bitset, add encode() and decode() functions for dense unaligned encoding of small integer values.
[x] 12. Implement CSV write and read functionality using the BCSV API and infrastructure 

13. Update Tools and Libraries to use new API
    Status: COMPLETED (actionable items done; C#/Unity and CLI→C_API deferred as low-priority)
    [x] CLI tools: ZoH auto-detection is RESOLVED — Reader uses RowCodecDispatch + FileCodecDispatch,
        automatically selects correct row/file codec from file header flags. All CLI tools (bcsv2csv,
        bcsvHead, bcsvTail, bcsvHeader) work with any codec combination without manual flags.
    [x] Python benchmarks: python/benchmarks/run_pybcsv_benchmarks.py exists ✓
    [x] C# benchmarks: csharp/benchmarks/Program.cs exists ✓
    [x] Python: pybcsv synced — FileFlags + compression already exposed; CsvWriter/CsvReader bindings
        added to bindings.cpp, __init__.py updated, headers synced. 77 Python tests pass.
        Fixed interop test expectations for RFC 4180 quoting.
    [DEFERRED] C#/Unity: no standalone library/NuGet package — only raw P/Invoke benchmark exists.
        Large effort, no active customer need. Defer until Unity support becomes a requirement.
    [DEFERRED] Update CLI tools to use C_API — currently use C++ API directly, which is acceptable.
        C API lacks CsvReader/CsvWriter. No functional benefit to porting.

[x] 14. Implement new row encoding schema (Delta Encoding), which uses variable length encoding for float and integer values
    Status: COMPLETE — Delta001 committed f48a51d, then optimised to Delta002 and Delta001 removed.
    Delta002 improvements over Delta001:
        - Combined mode+length header field (saves 1 bit/col for 2-8 byte types)
        - Type-grouped column loops via forEachScalarType (compile-time dispatch)
        - Float XOR + leading-zero byte stripping
    Performance (50K rows, 14 profiles, median):
        - StrmLZ4+Delta: 1,238 Krow/s write, 1,861 Krow/s read, 2.3% of CSV size
        - 2× faster than Delta001 with equal or better compression
    Tests: 39 Delta002 unit tests + 5 file-codec integration tests, 553 GTest total
    Files: row_codec_delta002.h/hpp, row_codec_delta002_test.cpp
    Wire format: docs/archive/PHASE3_DELTA_REPORT.md
    Main usage of bcsv is the handling of time-series data. Thus we expect many recurring, monotonic falling or raising values (First-Order-Constant) or values that are similar (close) to previouse values. In many cases the full extend of a variable type is not required to encode the actual value.
    This feature is working on a per row level, not only a single cell/value as the current vle_encode / vle_decode functions do. It tries to provide a better balance between computational effort and compression and takes advantange on previouse data (time-series).
    
    Design goals, improve throughput and further reduction of file sizes:
    Concept:
    1. exploit delta encoding to shorten storage for large types 
    2. exploit variable length to shorten storage for large types (VLE/ZigZag, Gorilla)
    3. Special handling of constant values ( row(n).col(i) = row(n-1).col(i)), we refere to it as Zero-order-Hold (ZoH)
    4. Special handling of constant faling/raising values (row(n).col(i) = row(n-1).col(i) + (row(n-1).col(i) - row(n-2).col(i)), we refere t it as First-order-Constant (FoC)
    5. Use block encoding (jump tables) to avoid complex conditional trees.
    6. small overhead in worst case scenariouse excepted
    7. exploit ZoH short cut to skip row if row_header of the current row equals row_header of the previouse row exactly. (row_length = 0) 

    Implementation:
    Based on RowCodec-ZoH (copy code, no reference)
    Additionaly to storing previouse row values (data_), we need to store gradiant information grad_data_ (use similar data structure and access patterns as data_), no change for string and bool columns
    Consider using vector operations to calculate gradients as they need to be used and stored anyway. A conditional execution might just add cost with little value.
    row header to change:
        bits_ / bool section un changed.
        but for each data column we are going to store a small single digit number, instead of bit.
            0: Zero Order Hold, the value in this column hasn changed vs. previouse column --> no data stored in data section for this column
            1: First Order Constant, the value in this column is a linear extrapolation of the previouse two rows. --> no data stored in data section for this column
            2: 1 byte of data stored for this column in the data section
            3: 2 bytes of data stored for this column in the data section
            ...
    The value range and hence the number of bits required for to encode a column in the row_header depends on the type. i.e. uint8_t/int8_t can only have 1 byte. Given the encoding above valid values are 0,1,2 (requiers 2 bits)
    uint16_t/int16_t have value range 0,1,2,3 (4 bits) etc. 

    Codec Architecture.
        ZoH codec do be used as the base line for the row format.
        row_header to be extended to hold information about number of bytes required to encode a specific column value.
            length information only present for numerical columns 
            length information only present if value for this column has changed -> conditional on row_header has changed bit status
            value range for length to be considered i.e. 
                uint8_t/int8_t   value range is 0 to 1: 
                    0: means current row is linear interpolation from previouse 2 rows, no value stored.
                    1: means value is neiter constant (ZoH), nor linear interpolation (First Order Hold) thus it is encoded directly using one byte.
                uint16_t/int16_t value range is 0 to 2:
                    0: means current row is linear interpolation from previouse 2 rows, no value stored.
                    1-2: means value is neiter contstant (ZoH), nor linear interpolation (First Order Hold) thus it is encoded directly using 1 or 2 bytes. The actual value stored is the difference between current and previouse value, stored in VLE/zigzag encoding.

[x] 15. Update C_API to cover full functionality of BCSV C++ API
    - Ensure full test coverage of C_API and that we pass
    - Ensure benchmarks are in place for C_API include vectorized access (i.e. 3D double vectors, coordinates)
    - Ensure documentation of C_API is complete

[x] 16. Update CLI Tools to use latest BCSV API version [DONE]
    Status: Complete — all tools modernised, tested, CTest-integrated

    Completed:
    [x] Modernised all 7 CLI tools to latest BCSV API (shared cli_common.h)
    [x] Bug fixes B1-B5, A2, D1, S1 from critical review applied
        B1: bcsv2csv verbose output → stderr (was polluting stdout CSV)
        B2: bcsv2csv ReaderDirectAccess fallback to sequential Reader
        B3: bcsv2csv benchmark JSON → stderr
        B4: bcsvTail sequential header via CsvWriter (proper RFC 4180 quoting)
        B5: csv2bcsv --no-zoh → maps to delta (was incorrectly mapping to flat)
        A2: bcsvGenerator uses resolveCodecFlags() instead of manual flag building
        D1: CLI_TOOLS.md and SKILLS.md updated (csv_format_utils.h → cli_common.h)
        S1: Consistent arg.starts_with("-") in bcsvHead, bcsvTail, bcsvHeader
    [x] csv2bcsv: all verbose/summary/benchmark output → stderr (stdout now clean CSV-only)
    [x] bcsvHeader -v: displays Row codec and File codec names (codecNamesFromFlags)
    [x] Reader::fileFlags() public accessor added
    [x] cli_common.h: CodecNames struct + codecNamesFromFlags() reverse mapping
    [x] Integration test: tests/test_cli_tools.sh (95 tests, all passing)
        Covers: csv2bcsv, bcsv2csv, bcsvHead, bcsvTail, bcsvHeader, bcsvGenerator
        Includes: pipeline tests, B1-B5 regression, 3×5 codec matrix round-trip,
        bcsvTail direct-access delta/ZoH, bcsvHeader codec name validation,
        edge cases (special chars, single-row, empty file, precision/negative)
        Semantic CSV comparison via csv_normalize()/csv_equal() helpers
    [x] Integration test: tests/test_bcsvSampler.sh (23 tests, all passing)
        Fixed: --no-zoh → --row-codec delta; harmonised set -uo pipefail
    [x] CTest registration for both shell integration tests (tests/CMakeLists.txt)
    [x] Workarounds removed: test scripts now use library-default codecs
        (delta + packet_lz4_batch); LIB-1/LIB-2 fixes verified
    [x] Documentation of library bugs: docs/BUG_REPORT_DirectAccess.md
    [x] All 7 tools build cleanly, 694 GTest + 87 C-API + 824 C-API-full pass

    Known issues (library-level, found during Item 16 testing):
    [LIB-1] FIXED — ReaderDirectAccess + delta row-codec (watermark decode)
    [LIB-2] FIXED — ReaderDirectAccess + packet_lz4_batch (seekToPacket added)
    [LIB-3] FIXED — FileCodecPacketLZ4Batch001::shutdownBgThread() race condition
        The background decompression thread's post-task "bg_task_ = IDLE" could
        overwrite the main thread's "bg_task_ = SHUTDOWN", causing a deadlock.
        Fix: waitForBgIdle() before setting SHUTDOWN in shutdownBgThread().
    See: docs/BUG_REPORT_DirectAccess.md

[x] 17. bcsvValidate — CLI tool to validate BCSV files (structure and content)
    Status: COMPLETE

    Completed:
    [x] Step 1: Added `creation_time` (uint64, Unix epoch) to FileHeader ConstSection
        — 24-byte ConstSection (was 16), format version bumped to v1.4.0
        — Writer sets creation_time automatically on open()
        — FileHeader: getCreationTime() / setCreationTime() accessors
    [x] Step 2: Moved bench_datasets.hpp to src/shared/test_datasets.hpp
        — Forwarding header left in benchmark/src/ for backward compatibility
        — 14 deterministic DatasetProfile definitions shared across tools/tests/benchmark
        — Fixed M_PI → std::numbers::pi (C++20 portable)
    [x] Step 3: Extracted RoundTripValidator to src/shared/validation.h
        — namespace bcsv_validation, cell-by-cell comparison with float tolerance
        — compareRow(), summary(), toJson(), errorCount(), passed()
    [x] Step 4: Created src/tools/bcsvValidate.cpp (~500 lines)
        — Mode 1 (Structure): header, version, creation_time, codecs, packet stats, layout
        — Mode 2 (Pattern): regenerates expected data from 14 benchmark profiles, compares cell-by-cell
        — Mode 3 (Comparison): bcsv-vs-bcsv and bcsv-vs-csv row-by-row comparison
        — C++20 std::chrono for timestamp formatting (no POSIX gmtime_r)
        — --json output, --deep structural walk, --tolerance float comparison
        — --list to show available profiles, --max-errors to cap mismatch output
    [x] Step 5: CMake integration (src/tools/CMakeLists.txt, benchmark/CMakeLists.txt, tests/CMakeLists.txt)
    [x] Step 6: Integration test suite — tests/test_bcsvValidate.sh (52 tests)
        — Mode 1: text, JSON, --deep, format_version, packet_size, total_packets, avg_rows_per_packet
        — Mode 2: pass, fail (wrong rowcount), wrong profile, unknown profile
        — Mode 3: bcsv-vs-bcsv identical, layout mismatch, bcsv-vs-csv round-trip
        — Error handling: missing file, no input, unknown option
        — Pipeline: generate → structure → pattern → csv roundtrip
        — 10/10 stability runs pass
    [x] Step 7: CTest registration (bcsvValidate_integration)
    [x] Step 8: Documentation updated
        — ARCHITECTURE.md: 24-byte ConstSection, creation_time, bcsvValidate in tool list
        — CLI_TOOLS.md: 8-tool overview table, examples, source structure
        — SKILLS.md: 8-tool target table, quick reference examples
    [x] Reader::fileHeader() public accessor added
    [x] Reader::fileFooter() public accessor (ReaderDirectAccess)
    [x] All 8 tools build cleanly, 694 GTests + 95 CLI + 39 Sampler + 52 Validate pass

[x] 18. project folder cleanup, skill improvement, CI/CD review, cleanup and improvement (LEAN DEVELOPMENT)
    Status: COMPLETE

    Phase 1 — Dead Artifact Cleanup:
    [x] Removed 3 empty directories (test_error_handling_temp/, bcsv_test_files/codec_dispatch/, docs/archive/)
    [x] Removed orphaned vle_test.cpp from build (uses obsolete VLE API; current API covered by vle_template_test.cpp)
    [x] Verified .gitignore covers tmp/, __pycache__/, build/

    Phase 2 — Documentation Consolidation:
    [x] Consolidated 5 satellite SKILLS.md files into root SKILLS.md "Subsystem Quick Reference" section
        Deleted: examples/SKILLS.md, python/SKILLS.md, src/tools/SKILLS.md, tests/SKILLS.md, unity/SKILLS.md
    [x] Merged 8 per-tool .md files + overview into single src/tools/CLI_TOOLS.md (~380 lines)
        Deleted: csv2bcsv.md, bcsv2csv.md, bcsvHead.md, bcsvTail.md, bcsvHeader.md, bcsvSampler.md, bcsvGenerator.md, bcsvValidate.md
    [x] Fixed ARCHITECTURE.md file format version: 1.3.0 → 1.4.0 (matches definitions.h)
    [x] Deduplicated README.md: replaced 4 language code snippets with compact table + links to API_OVERVIEW.md
    [x] Condensed ERROR_HANDLING.md: removed ~100 lines of redundancy (repeated patterns, broken archive links, restating conclusion)

    Phase 3+4 — AI Instruction Files (model-agnostic, with embedded command reference):
    [x] Created .github/copilot-instructions.md (~95 lines) — build commands, project layout, naming conventions, design patterns
    [x] Created .claude.md — same content for Claude Code
    [x] Created AGENTS.md — same content for Codex / VS Code Agent Mode

    Phase 5 — CI/CD Fixes:
    [x] release.yml: fixed heredoc indentation bug — version_generated.h was written with leading whitespace (invalid C++)
    [x] benchmark.yml: fixed command injection risk on workflow_dispatch inputs (now uses env vars),
        removed unused pull-requests:write permission, added src/** to path filters
    [x] build-and-publish.yml: fixed inconsistent actions/checkout version (v4 → v5)

    Verification: 694/694 GTests pass, 87/87 C API tests pass, build clean (ninja-debug)


19. code cleanup
    - remove ZOH codec?
    - updated and condense documentation (remove duplications)
    - consider refactorings:
        readability
        maintainability
    - API clean up, identifiy functions that can be implemented easily from other functions?

20. multi platform build
    - linux gcc, clangd
    - windows MSCV
    - apple clangd-homebrew?

21. bcsvSampler performance
    Status: IN PROGRESS — Phase 1 (dispatch optimisation) complete

    Completed:
    [x] Baseline benchmarks captured (100K rows, median, release build):
        Reader_Baseline: 13,349 Krow/s, Sampler_Passthrough: 5,092 Krow/s,
        FloatFilter: 7,777, Gradient: 4,705, MovingAvg3: 4,776, StringFilter: 6,104
    [x] Passthrough fast-path: detect trivial conditional ("true") and identity
        selection ("X[0][*]") at compile time; bypass VM entirely for passthrough.
        Result: Passthrough 5,092 → 13,059 Krow/s (+156%), matching raw Reader speed.
        Also activates when no conditional/selection is set (common CLI case).
        Files: sampler.h (cond_passthrough_, sel_passthrough_ flags, detection decls),
               sampler.hpp (detectCondPassthrough, detectSelPassthrough, fast-path in next())
    [x] Computed-goto dispatch (GCC/Clang): replace switch(op) with threaded
        goto *dispatch_table[op] using labels-as-values extension. MSVC fallback
        retains original switch. Bounds-checked, -Wpedantic suppressed.
        Result: ~5-10% improvement on short bytecode programs (StringFilter).
        File: sampler_vm.hpp (dispatch table, 64 label handlers, VM_DISPATCH macro)
    [x] Quality assurance: 694 GTests, 39 sampler integration, 95 CLI integration,
        700/700 CTest — all passing. Zero warnings on GCC release build.

    Next steps:
    [ ] Profiling: Create perf profiling script to identify actual bottlenecks
        (VM dispatch vs Row::get vs I/O). Use perf stat / perf record + flamegraph.
    [ ] Vectorisation: Consider compiler auto-vectorisation for batch row processing
        (process N rows through same bytecode, form SIMD lanes per column type).
        User preference: rely on compiler auto-vectorisation, no explicit intrinsics.
    [ ] Pipeline parallelism: If profiling shows I/O-bound or decode-bound bottleneck,
        consider multi-thread pipeline (read+decompress → decode → sample → encode → write).
    [ ] Function-pointer dispatch: Alternative to bytecode — compile expression into
        vector of std::function or typed function pointers. Higher memory, but eliminates
        decode overhead. Evaluate after profiling confirms dispatch is bottleneck.



22. Update pybcsv to utilize latest bcsv version, supporting all codecs and major API entry points (lets work together to identify which to include, start broad, remove bloat/complexity/internals)
    - full test coverage and validated
    - pypi and github CI/CD pipline is operational
    - pandas and numpy support
    - benchmarking in place read to/write from pandas
    - benchmarking in place read to/write from numpy,
    - benchmarking in place read to/write from python (plain)

23. Update C# bindinds, using latest version of bcsv library, supporting all codecs and major API entry points (lets work together to identify which to include, start broad, remove bloat/complexity/internals)
    - full test coverage and validated
    - create and publish nuget package
    - valdiate C# bindings work with unity

24 Compare against (document and benchmarks):
        parquet
        csv
        arrow
        influxDb
        excel

25. Write paper and release/submit --> WIKI page on GitHub

Parking LOT:
26. Create, demonstarte/validate a concept to maintain backward compatibility (read old BCSV files with new version of the library)

27. dictionary encoding for strings
    For strings a low cardinality is expected (many repetitions of the same string) are expected. Thus a dictionary based compression should be implemented. Exploit similar approach as for numerical values.
    Store string at its first occurens and assign ID (simply increment number, within the current packet)
    Other occurences just to reference that number. Run dedicated dicts per row, to balance speed and compression ratio. Block wide compression may come at a later point.

28. Create sampler function / API to select data based on certain conditions: 
    - Support C++ interface (iterate over individual rows)
    - Support Python interface (iterate over individual rows and block reads (pandas/numpy))
29. Make BCSV eco system to use piping features CLI, compareable to cat, more, sed

22. CLI tools:
    - Modify CLI tools to utilize direct read functionality to avoid parsing the entire file (when possible):
        - bcsvTail
        - bcsvHead
        - bcsv2csv in combiniation with slicing
    - create: bcsv2Parquet
    - create: parquet2bcsv
    - create: bcsvInspect   (checks if the file is complet or damaged (i.e. index missing), can repair file/index, prints information on the file content)
    - create: bcsvSampler   (creates an down sampled version, using specific rules provided)
    - create: bcsvMore
    - create: bcsvCat
    - create: bcsvSed
    - create: bcsvCompress  (insepects the file and tries to apply a more agressive compression, using 2-phase inspection and a file wide dictionary)
    - create: bcsvIndex     (tool to rebuild index)
    - create: bcsvConvert   (converts from one bcsv file format to another, use to rebuild index or improve compression)

23. Implement Column Modifiers
    provides hints on what encoding schema to be used for this particular column
    modifiers to consider:
        index:      unique numbers
        volatile:   numbers are scattered, do not apply delta encoding
        monotonic:  apply higher order delta encoding as a "nearly" monotonic (smooth) behaviour is expected
        ordered:    data to fall or grow in ordered fashion 

24. Extend Direct Access Reader to support sparse column reads
        1.[sparse rows, dense columns]: Read fully populated row at specific predfined row indices, includes reading sequentialy within a row segment (row_start:row_end)
        2.[sparse rows, sparse columns]: Read a few columns per row, at specific predefined row indices, includes reading sequentialy within a row segment (row_start:row_end)
        3.[dense rows, spares columns]: Read a few columns per row, going over the entire file.
    
        Some thoughts:
            - use RowView to avoid deserilizing all columns if not needed
            - consider constraining string dictionary encoding to individual columns (lets do that as default, make blockwide (multi column) dictionary compression a future feature)
            - use Template parameters to "punch out" inividual columns

25. Sampler functionality:
- Sampler to support conditional assignments i.e. conditional = { {x[0][0] > 1},{x[0][0]<=1} }, selection = {x[0][*], x[-1][*]}
- Sampler to support wild cards i.e. to output all columns of row.
- Sampler to provide index based conditions i.e. every 5th row.

26. BCSV usability
- compare BCSV csv reader and writter against 3rd party implementations that are considered industry standards/references
- BCSV to support arbitary streams for I/O, not just files. i.e. support cin and cout to enable piping.
- BCSV CLI tools to support piping, document and present
- demonstrate how BCSV can be used for network I/O i.e. in combination with UDP/TCP (documentation, example codes)

27. BCSV stability and Robustness
    confirm project can be build with out errors and warnings on windows(MSCV), linux(GCC/clangd) and apple(dlangd). 
    confirm correct operations in debug and release mode on all 3 platforms and compilers
    confirm performance on linux, windows, apple, stm32 and xilinix/AMD -> ZYNQ, VERSAL and KRIA

28. BCSV Performance:
- micro optimization on rowDelta serialization and deserialization
    review assembler generate, --> inlining?, unrolling?
    explore opportunities to help the branchprediciton, 
    explore opportunities to use SIMD vectorization if multiple columns have same data type 
- consider runtime compilation (comparable to GPU shaders) for serialization and deserialization to create optimized kernels for specific layouts and architecture.
- perhaps make it a CMAKE configuration to either use the curren C++ only implementation (no deppendencies) and alternativly allow for in place compiler using LLVM or similar.
- consider vectorization of hot loops in serialization and deserialization
- consider multithreading for complex filters to build a pipleine like:
        load -> uncompress -> deserialization -> sampling -> serialization -> compression -> save running on multiple CPUs connected through FIFOs. 
        Consider throughput (rows/sec) and efficency (watt/row)